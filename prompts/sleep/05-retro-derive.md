# Post-Retro Derivation — Core Knowledge Update

From today's retrospective, update persistent knowledge files.

## Input

${RETRO_CONTENT}

## Task 1: Core Knowledge Maintenance

**SOUL.md is read-only** — never modify it. Identity is human-managed.

1. Read `agent_notes.md`, `user_profile.md`, and `core_facts.md`.
2. Remove entries that are outdated or contradicted by today's retro.
3. Update entries that have become stale based on recent interactions.
4. Keep files concise — no redundancy.

## Task 2: Append New Agent Notes

From the retro above, identify NEW persistent rules or lessons not already in `agent_notes.md`:
- Behavioral rules ("never do X", "always check Y first")
- User preferences discovered today
- Recurring mistakes with a clear fix
- Project context that future sessions need

For each candidate, check if `agent_notes.md` already contains it (same meaning, even if different wording). Skip duplicates. Append only genuinely new items to the appropriate section.

If nothing new to add, say "No new items" and move on.

Report what was changed (if anything).
